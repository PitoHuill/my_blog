# 个人博客项目说明

> 最后更新：2026-07-18
> 当前分支：`codex/bilingual-site`
> 当前状态：英文默认站与 `/zh/` 中文站的页面、文章、搜索、RSS、SEO 元数据和语言切换已完成；站点仍是 Astro 静态构建，可部署到 GitHub Pages 根路径或项目子路径。

## 一、双语架构与完成进度

站点采用“共享组件 + 显式语言路由 + 成对内容”的结构，不使用运行时翻译接口，也不在浏览器中自动跳转：

```text
src/i18n/config.ts          语言类型、默认语言和语言路径规则
src/i18n/ui.ts              导航、页面标题、搜索等共享 UI 文案
src/data/site.ts            双语个人资料与项目数据
src/content/posts/en/*.md   英文文章（默认、无 URL 前缀）
src/content/posts/zh/*.md   中文文章（/zh/ 前缀）
src/components/pages/*      两种语言复用的页面组件
src/pages/*                 英文薄路由
src/pages/zh/*              中文薄路由
src/lib/paths.ts            BASE_PATH 与语言前缀的统一链接助手
```

已完成的双语能力包括：

- 首页、文章列表/详情、项目列表/详情、关于页和搜索页；
- 英文 `/rss.xml` 与中文 `/zh/rss.xml`，两个订阅源不会混入另一语言内容；
- 页面级 `html lang`、canonical、`hreflang=en`、`hreflang=zh-CN` 和 `hreflang=x-default`；
- 可访问的语言切换控件：有对应翻译时跳到等价页面，没有对应翻译时明确显示禁用状态；
- 根路径和 `BASE_PATH=/my_blog` 两种静态构建，内部链接只添加一次基础路径。

## 二、路由表

英文是默认语言，不使用 `/en/`；中文统一放在 `/zh/` 下。

| 功能 | 英文 | 中文 |
| --- | --- | --- |
| 首页 | `/` | `/zh/` |
| 文章列表 | `/posts/` | `/zh/posts/` |
| 文章详情 | `/posts/<slug>/` | `/zh/posts/<slug>/` |
| 项目列表 | `/projects/` | `/zh/projects/` |
| 项目详情 | `/projects/<slug>/` | `/zh/projects/<slug>/` |
| 关于 | `/about/` | `/zh/about/` |
| 搜索 | `/search/` | `/zh/search/` |
| RSS | `/rss.xml` | `/zh/rss.xml` |

## 三、添加一对翻译文章

一对文章必须使用相同文件名（即相同公开 `slug`）和相同 `translationKey`，但 `locale` 分别写成 `en` 与 `zh`。

1. 创建英文文件：`src/content/posts/en/my-post.md`。
2. 创建中文文件：`src/content/posts/zh/my-post.md`。
3. 两个文件都填写相同的 `translationKey: my-post`，并保持相同文件名 `my-post.md`。
4. 英文文件填写 `locale: en`，中文文件填写 `locale: zh`。
5. 分别填写本语言的标题、摘要、标签、系列和正文；发布日期可相同，也可按实际发布计划填写。

英文 frontmatter 示例：

```yaml
---
title: My Post
description: English summary.
pubDate: 2026-07-18
tags: [Astro]
locale: en
translationKey: my-post
---
```

中文文件使用相同字段，其中关键配对字段为：

```yaml
locale: zh
translationKey: my-post
```

构建后对应地址是 `/posts/my-post/` 与 `/zh/posts/my-post/`。如果暂时只有一种语言，可以只创建单篇文章；该文章页面的语言控件会显示为禁用链接，避免跳到不存在的页面。

## 四、更新双语资料、项目和 UI 文案

- 个人资料：编辑 `src/data/site.ts` 中 `profile.tagline`、`profile.bio` 与 `profile.now` 的 `en`/`zh` 值；姓名、邮箱和 GitHub 地址是共享字段。
- 项目内容：编辑 `src/data/site.ts` 中每个项目的 `title`、`status`、`description` 与 `body` 的 `en`/`zh` 值；项目 `slug` 与年份由两种语言共享。
- 共享界面文案：编辑 `src/i18n/ui.ts`，同一个键必须同时提供英文和中文。现有单元测试会检查两种语言的键值数量和非空状态。
- 页面专属文案：优先放进 `src/i18n/ui.ts` 并由共享页面组件按 `locale` 读取，不要在英文/中文路由包装器里复制页面实现。
- 新链接：使用 `localizedPath()`；仅需要基础路径、不需要语言前缀的静态资源使用 `withBase()`，不要手工拼接 `/my_blog` 或 `/zh`。

## 五、本地验证命令

安装依赖并运行开发服务器：

```powershell
npm install
npm run dev
```

运行单元测试与完整 E2E：

```powershell
npm run test:unit
npm run test:e2e
```

普通生产构建：

```powershell
npm run build
```

模拟 GitHub Pages 项目子路径构建：

```powershell
$env:BASE_PATH='/my_blog'
npm run build
Remove-Item Env:BASE_PATH
```

子路径构建后重点检查：

- `dist/search/index.html` 与 `dist/zh/search/index.html` 中的数据和结果链接只属于当前语言；
- `dist/rss.xml` 与 `dist/zh/rss.xml` 中的频道、文章标题、摘要和链接只属于当前语言；
- canonical、hreflang、导航、语言切换和静态资源链接均包含一次 `/my_blog`；
- 输出中不存在 `/my_blog/my_blog/`、`/zh/zh/` 或指向不存在内部文件的链接。

## 六、后续迭代建议

1. 用真实个人简介、项目和文章替换示例内容，并为核心文章补齐成对翻译。
2. 在 CI 中同时执行单元测试、Playwright E2E、普通构建和子路径构建检查。
3. 添加 sitemap、结构化数据、社交分享图片和更完整的可访问性审计。
4. 文章数量明显增长后，再考虑搜索高亮、分词或预生成索引；当前客户端静态过滤适合小型内容库。
5. 保持 `translationKey` 配对规则稳定，必要时增加构建期校验，主动报告缺失或重复的翻译键。
