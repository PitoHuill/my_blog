# 个人博客项目总结

> 最后更新：2026-07-11  
> 当前分支：`main`  
> 当前状态：核心页面与 GitHub Pages 工作流已完成，等待持续补充真实内容与后续体验优化。

## 一、项目定位

这是一个使用 Astro 构建的静态个人博客，视觉方向参考深色学术型个人主页：克制的排版、细边框卡片、低饱和配色和双栏信息结构。

项目优先保证三件事：

1. 内容容易维护，不需要数据库或后台服务。
2. 桌面端保留双栏布局，移动端自动变为单栏。
3. 可以直接通过 GitHub Pages 免费部署，并支持日间/夜间主题切换。

## 二、已完成内容

### 页面与布局

- 双栏首页：左侧个人资料、头像、统计与精选链接，右侧文章和项目内容。
- 响应式布局：桌面端双栏，移动端单栏并调整侧栏卡片排列。
- 顶部导航：首页、文章、项目、关于，以及图标化的主题切换按钮。
- 页脚：站点信息、RSS 入口与基础版权信息。
- 文章列表页、文章详情页。
- 项目列表页、项目详情页。
- 关于页面，包含个人介绍和 Now 状态模块。
- 本地静态搜索页，可按标题、摘要和标签过滤文章。

### 视觉与交互

- 深色主题为默认视觉方向，同时支持浅色主题。
- 使用 CSS 变量统一管理背景、文字、边框、强调色和阴影。
- 主题切换使用月亮/太阳图标，不占用导航文字空间。
- 个人头像已替换为 `public/avatar.jpg`。
- 卡片悬停、键盘焦点、移动端适配和减少动画偏好已处理。
- 页面设置了基础 SEO 元信息、canonical 链接和 Open Graph 信息。

### 内容与数据

目前内容统一维护在：

```text
src/data/site.ts
```

其中包含：

- `profile`：姓名、简介、Now 状态、邮箱和 GitHub 链接。
- `posts`：文章标题、日期、摘要、标签和正文段落。
- `projects`：项目名称、年份、状态、简介和正文。

### 发布与工程配置

- Astro 静态输出模式已配置。
- GitHub Pages Actions 工作流已配置：`.github/workflows/deploy.yml`。
- 工作流会自动识别仓库类型：用户主页仓库使用根路径，普通项目仓库使用 `/仓库名` 路径。
- 已配置 RSS：`/rss.xml`。
- 已配置 favicon。
- `.gitignore` 已忽略 `.superpowers/`、`.worktrees/`、依赖、构建产物和 Astro 缓存。

## 三、当前进度

| 模块 | 状态 | 说明 |
| --- | --- | --- |
| Astro 项目骨架 | 已完成 | 可安装依赖并运行开发服务器 |
| 首页双栏布局 | 已完成 | 桌面端双栏，移动端响应式 |
| 日间/夜间主题 | 已完成 | 使用图标切换并记忆用户选择 |
| 文章页面 | 已完成 | 列表、详情、标签展示 |
| 项目页面 | 已完成 | 列表与详情页 |
| 关于页面 | 已完成 | 个人介绍与 Now 模块 |
| 搜索 | 已完成基础版 | 客户端静态过滤，后续可增强 |
| RSS | 已完成 | 静态 RSS 输出 |
| 头像 | 已完成 | 使用 `public/avatar.jpg` |
| GitHub Pages 工作流 | 已完成 | 需在仓库 Pages 设置中启用 GitHub Actions |
| 内容完善 | 进行中 | 当前仍有示例文章和示例项目 |
| 自动化测试 | 待完善 | 已有 Playwright 配置，需修复测试文本编码并接入 CI |

最近一次本地验证：`npm run build` 成功，生成 11 个静态页面。

## 四、主要文件说明

```text
src/
├─ components/              顶部导航、页脚、主题切换
├─ data/site.ts             个人资料、文章、项目内容数据
├─ layouts/BaseLayout.astro 页面公共布局与 SEO
├─ pages/index.astro        首页
├─ pages/about.astro        关于页面
├─ pages/search.astro       搜索页面
├─ pages/posts/             文章列表与详情
├─ pages/projects/          项目列表与详情
└─ styles/global.css        全局主题变量与基础样式

public/avatar.jpg           个人头像
public/favicon.svg          网站图标
.github/workflows/deploy.yml GitHub Pages 部署工作流
```

## 五、已知限制与注意事项

1. 当前文章正文存储在 TypeScript 文件中，文章数量增加后可维护性会下降。
2. 搜索是客户端静态过滤，适合小规模文章库，不适合全文检索大量文章。
3. 头像通过 CSS 背景图显示，替换图片时保持文件名为 `public/avatar.jpg` 最简单。
4. GitHub Pages 必须在仓库 `Settings → Pages` 中将 Source 设置为 **GitHub Actions**。
5. 如果使用自定义域名，需要同步调整工作流中的 `SITE_URL`。
6. Actions 中关于 Node.js 20 的提示是弃用警告，目前不会阻止构建；后续应升级相关 action 版本。

## 六、下一轮迭代建议

### P0：先完善内容与发布闭环

- 将 `src/data/site.ts` 中的示例个人资料、邮箱、文章和项目替换为真实内容。
- 在 GitHub Pages 上确认 `main` 分支部署成功，并记录最终访问地址。
- 修复 Playwright 测试中的中文编码问题，至少覆盖首页、主题切换和文章详情跳转。
- 补充 3～5 篇真实文章，验证长标题、长摘要和移动端排版。

### P1：提升内容维护体验

- 使用 Astro Content Collections，将文章迁移到 `src/content/posts/*.md`。
- 支持 Markdown 标题、代码块、引用、图片和目录。
- 增加文章阅读时间、上一篇/下一篇和相关文章推荐。
- 搜索结果增加高亮、空状态和快捷键提示。
- 增加文章封面或项目缩略图，但保持低装饰、低干扰的视觉风格。

### P2：提升质量与可发现性

- 接入 Giscus 评论或其他基于 GitHub Discussions 的评论方案。
- 增加 sitemap、结构化数据和更完整的社交分享图片。
- 升级 GitHub Actions 使用的 action 版本，消除 Node.js 20 弃用警告。
- 在 CI 中执行 `npm run build` 和 Playwright 测试。
- 检查键盘导航、颜色对比度和图片替代文本，完善无障碍体验。

## 七、本地开发与发布

```powershell
cd "C:\Users\pito\Documents\Presentation\.worktrees\astro-personal-blog"
npm install
npm run dev
```

构建检查：

```powershell
npm run build
```

提交发布：

```powershell
git add .
git commit -m "content: update blog"
git push origin main
```

推送到 `main` 后，GitHub Actions 会自动构建并部署。若只修改本地内容，优先在本地开发服务器确认，再提交推送。

## 八、下一次迭代的建议顺序

```text
补充真实内容
    ↓
确认 GitHub Pages 正常访问
    ↓
修复并接入自动化测试
    ↓
迁移到 Markdown 内容集合
    ↓
补充评论、SEO 与文章阅读体验
```

当前项目已经具备继续写作和部署的基础。下一阶段重点应从“搭建页面”转向“稳定发布内容”和“降低长期维护成本”。
