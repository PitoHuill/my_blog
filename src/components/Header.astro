---
import ThemeToggle from './ThemeToggle.astro';
import { profile } from '../data/site';
import { withBase } from '../lib/paths';
---
<header class="site-header"><div class="shell header-inner"><a class="brand" href={withBase('')}><span>◒</span>{profile.name}</a><nav aria-label="主导航"><a href={withBase('')}>首页</a><a href={withBase('posts/')}>文章</a><a href={withBase('projects/')}>项目</a><a href={withBase('about/')}>关于</a></nav><ThemeToggle /></div></header>
